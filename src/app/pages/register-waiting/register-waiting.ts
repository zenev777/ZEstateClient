import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-register-waiting',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './register-waiting.html',
})
export class RegisterWaiting {}
